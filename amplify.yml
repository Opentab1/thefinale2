version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd dashboard/ui
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dashboard/ui/build
    files:
      - '**/*'
  cache:
    paths:
      - dashboard/ui/node_modules/**/*
