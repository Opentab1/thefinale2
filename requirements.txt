# Pulse 1.0 - Python Dependencies

# Core Framework
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5
python-socketio==5.10.0
eventlet==0.33.3
pyyaml==6.0.1
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23

# Hardware & Sensors
RPi.GPIO==0.7.1
smbus2==0.4.3
picamera2==0.3.17
pyaudio==0.2.14
board==1.0
adafruit-circuitpython-bme280==2.6.23

# Computer Vision & AI
# Use OpenCV versions with NumPy 2.x compatibility and prebuilt aarch64 wheels
opencv-python>=4.10.0,<5
# Pillow 11.x supports Python 3.13 with prebuilt wheels on aarch64
pillow>=11.0.0,<12
# NumPy 2.2+ provides Python 3.13 wheels; avoid legacy 1.x which builds from source
numpy>=2.2.0,<3

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# Smart Home Integrations
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
phue==1.1
spotipy==2.23.0
# Note: HDMI-CEC control is handled via the system package `cec-utils` and the `cec-client`
# CLI; no Python package required here.

# Utilities
requests==2.31.0
schedule==1.2.0
cryptography==41.0.7
psutil==5.9.6
