# Pulse 1.0 - Python Dependencies

# Build Dependencies (must be installed first for Python 3.13)
setuptools>=65.0.0
wheel>=0.38.0

# Core Framework
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5
python-socketio==5.10.0
eventlet==0.33.3
pyyaml==6.0.1
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23

# Hardware & Sensors
RPi.GPIO==0.7.1
smbus2==0.4.3
picamera2==0.3.17
pyaudio==0.2.14
board==1.0
adafruit-circuitpython-bme280==2.6.23

# Computer Vision & AI
opencv-python==4.8.1.78
# Pillow 10.x fails to build on Python 3.13 (no wheels, metadata error)
# Pin to 11.x which supports Python 3.13 and provides prebuilt wheels on aarch64
pillow>=11.0.0,<12
# numpy 1.24.3 doesn't have wheels for Python 3.13 - use 1.26+ which supports 3.13
numpy>=1.26.0,<2.0.0
# tflite-runtime removed: no Python 3.13 wheels available yet
# Will be re-added when Python 3.13 support is available

# Audio Processing
librosa==0.10.1
soundfile==0.12.1
pydub==0.25.1

# Smart Home Integrations
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
phue==1.1
spotipy==2.23.0
# cec==0.2.7 - REMOVED: Incompatible with libcec 7.0+
# CEC functionality provided by cec-client CLI tool (installed via cec-utils apt package)

# Utilities
requests==2.31.0
schedule==1.2.0
cryptography==41.0.7
psutil==5.9.6
