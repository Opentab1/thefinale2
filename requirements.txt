# Pulse 1.0 - Python Dependencies

# Build Dependencies (must be installed first for Python 3.13)
setuptools>=65.0.0
wheel>=0.38.0

# Core Framework
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5
python-socketio==5.10.0
eventlet==0.33.3
pyyaml==6.0.1
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.23

# Hardware & Sensors
RPi.GPIO==0.7.1
smbus2==0.4.3
picamera2==0.3.17
pyaudio==0.2.14
board==1.0
adafruit-circuitpython-bme280==2.6.23

# Computer Vision & AI
# Upgrade OpenCV to a version with Python 3.13 wheels and NumPy 2.x support
opencv-python>=4.10.0
# Pillow 11.x supports Python 3.13 and provides prebuilt wheels on aarch64
pillow>=11.0.0,<12
# Use NumPy 2.x which provides Python 3.13 wheels for aarch64
numpy>=2.1.0,<3.0.0
# tflite-runtime removed: no Python 3.13 wheels available yet
# Will be re-added when Python 3.13 support is available

# Audio Processing (none required at install time)
# Removed librosa/soundfile/pydub due to NumPy 2.x incompatibility and because
# they are not used by the current codebase.

# Smart Home Integrations
google-auth==2.25.2
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
phue==1.1
spotipy==2.23.0

# Utilities
requests==2.31.0
schedule==1.2.0
cryptography==41.0.7
psutil==5.9.6
