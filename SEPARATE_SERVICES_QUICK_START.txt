╔══════════════════════════════════════════════════════════════╗
║     🎯 SEPARATE SERVICES - QUICK DEPLOYMENT                  ║
╚══════════════════════════════════════════════════════════════╝

✅ PROBLEM SOLVED:
   Camera crashes no longer kill audio or dashboard!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 DEPLOY (Run on your Raspberry Pi):

cd /opt/pulse
git pull origin cursor/debug-and-stabilize-audio-features-on-raspberry-pi-702d
bash install_separate_services.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ VERIFY (All 3 services should be running):

systemctl status pulse-audio pulse-camera pulse-hub-main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 MONITOR:

# Audio (should be stable now - no restarts!)
sudo journalctl -u pulse-audio -f | grep -E "(🔊|🎵)"

# Camera (may restart but won't affect others)
sudo journalctl -u pulse-camera -f

# Hub (dashboard + sensors)
sudo journalctl -u pulse-hub-main -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 WHAT YOU'LL SEE:

Audio Service:
🔊 Measured decibel level: 45.3 dB  (every 10s)
🎵 Song detected: Title - Artist     (every 60s)
✅ STABLE - No restarts!

Camera Service:
📷 Occupancy: 2
⚠️  May crash every 1-2 min (libcamera bug)
🔄 Auto-restarts in 5 seconds
✅ Doesn't affect audio or dashboard!

Hub/Dashboard:
🌐 Dashboard: http://your-pi:8080
🌡️  Temperature: 72°F
✅ Keeps running during camera restarts!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 BENEFITS:

✅ Audio runs continuously (no 1-2 min restarts!)
✅ Camera crashes isolated (only affects people counting)
✅ Dashboard always accessible
✅ dB calibration fixed (now reads 40-60 in quiet office)
✅ Better fault isolation and debugging

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 CONTROL SERVICES:

# Start/stop all
sudo systemctl start/stop pulse.service

# Start/stop individual
sudo systemctl start/stop pulse-audio
sudo systemctl start/stop pulse-camera  
sudo systemctl start/stop pulse-hub-main

# Restart if needed
sudo systemctl restart pulse-audio

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 FULL GUIDE: SEPARATE_SERVICES_DEPLOYMENT.md

╚══════════════════════════════════════════════════════════════╝
